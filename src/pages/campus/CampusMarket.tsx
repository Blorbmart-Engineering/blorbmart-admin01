import MarketManager from '../../components/MarketManager'

/** The head of operations' own campus market. The server picks the campus. */
export default function CampusMarket() {
  return <MarketManager />
}
